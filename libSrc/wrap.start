;(function( root, factory ) {
    if ( typeof define === 'function' && define.amd ) {
        define([], function() {
            // Define both a global and an AMD module variable
            return root.myLib = factory();
        });
    } else {
        root.myLib = factory();
    }
}( this, function() {
